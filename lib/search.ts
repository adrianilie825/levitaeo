/**
 * @deprecated Use `@/lib/search-db` for server-side search and `/api/search` for client queries.
 * This module remains temporarily for reference during the Supabase catalog migration.
 */
export type {
  SearchResult,
  SearchResultStatus,
  SearchResultType,
  SearchSuggestion,
} from "@/lib/search-db";
